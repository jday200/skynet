

<div>
    <div class="main">

        <div class="main_list">

            <div class="main_channel">
                <fieldset>
                    <legend>
                        <a href="/house/"><b>House</b></a>
                    </legend>
                    <% if(houses){
                        houses.forEach(house =>{ %>
                    <div class="main_item">
                        <%=house.content%>
                    </div>
                     <% });
                    } %>
                </fieldset>
            </div>

            <div class="main_channel">
                <fieldset>
                    <legend>
                        <a href="/job/"><b>Job</b></a>
                    </legend>
                    <% if(jobs){
                        jobs.forEach(job =>{ %>
                    <div class="main_item">
                        <%=job.content%>
                    </div>
                     <% });
                    } %>
                </fieldset>
            </div>

        </div>

        <div class="main_list">

            <div class="main_channel">
                <fieldset>
                    <legend>
                        <a href="/exchange/"><b>Exchange</b></a>
                    </legend>
                    <% if(exchanges){
                        exchanges.forEach(exchange =>{ %>
                    <div class="main_item">
                        <%=exchange.content%>
                    </div>
                     <% });
                    } %>
                </fieldset>
            </div>

            <div class="main_channel">
                <fieldset>
                    <legend>
                        <a href="/trading/"><b>Trading</b></a>
                    </legend>
                    <% if(tradings){
                        tradings.forEach(trading =>{ %>
                    <div class="main_item">
                        <%=trading.content%>
                    </div>
                     <% });
                    } %>
                </fieldset>
            </div>

        </div>

        <div class="main_list">

            <div class="main_channel">
                <fieldset>
                    <legend>
                        <a href="/traffic/"><b>Traffic</b></a>
                    </legend>
                    <% if(traffics){
                        traffics.forEach(traffic =>{ %>
                    <div class="main_item">
                        <%=traffic.content%>
                    </div>
                     <% });
                    } %>
                </fieldset>
            </div>

            <div class="main_channel">
                <fieldset>
                    <legend>
                        <a href="/resource/"><b>Resource</b></a>
                    </legend>
                    <% if(resources){
                        resources.forEach(resource =>{ %>
                    <div class="main_item">
                        <%=resource.name%>
                    </div>
                     <% });
                    } %>
                </fieldset>
            </div>

        </div>

        <div class="main_list">

            <div class="main_channel">
                <fieldset>
                    <legend>
                        <a href="/content/articles.html"><b>Article</b></a>
                    </legend>
                    <% if(articles){
                        articles.forEach(article =>{ %>
                    <div class="main_item">
                        <%=article.title%>
                    </div>
                     <% });
                    } %>
                </fieldset>
            </div>

            <div class="main_channel">
                <fieldset>
                    <legend>
                        <a href="/content/blogs.html"><b>Blog</b></a>
                    </legend>
                    <% if(blogs){
                        blogs.forEach(blog =>{ %>
                    <div class="main_item">
                        <%=blog.content%>
                    </div>
                     <% });
                    } %>
                </fieldset>
            </div>

        </div>

    </div>
</div>